// Dummy implementation. Replace with your actual email/SMS logic.
export const sendAdminNotification = async (complaint) => {
  // Example: send email to admin
  console.log(
    `Complaint #${complaint._id} has not been resolved in 2 days. Please review and take action.`
  );
  // Integrate with your email/SMS service here
};
